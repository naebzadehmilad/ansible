# sshd.yml
          - replace:
                  dest: /etc/hosts
                  regexp: '127.0.0.1'
                  replace: '127.0.0.2'
                  backup: yes
                 # validate: '/usr/sbin/sshd -t'
         # - service:
          #        name: sshd
           #       state: reloaded
